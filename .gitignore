# macOS
.DS_Store

# Local Claude state — session CSV goes to CLAUDE_PLUGIN_DATA, not here,
# but guard against misconfiguration
adoption-os.csv

# Editor
.idea/
.vscode/
*.swp

# Microsoft Word lock files (created when a .docx is open)
~$*

# Generated Word doc — regenerated from the HTML via
# `textutil -convert docx BUILD_YOUR_OWN_PLUGIN.html -output BUILD_YOUR_OWN_PLUGIN.docx`
*.docx
