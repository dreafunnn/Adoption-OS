{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Read|Glob|Grep|Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/logger.sh\""
          }
        ]
      }
    ]
  }
}
